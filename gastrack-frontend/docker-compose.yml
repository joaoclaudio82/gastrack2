services:
  frontend:
    container_name: gastrack-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:80'
    restart: always
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://127.0.0.1:80']
      interval: 30s
      timeout: 10s
      retries: 3
