/**
 * Default page size used as initial value for paginated tables across the app.
 * Change this value to update the initial page size globally.
 */
export const DEFAULT_PAGE_SIZE = 5;

/**
 * Default selectable options for the "items per page" dropdown.
 */
export const DEFAULT_PAGE_SIZE_OPTIONS: readonly number[] = [5, 10, 25, 50];
